# This pyproject.toml is for FUTURE packaging only.
# Streamlit Cloud uses requirements.txt for app dependencies.
# Do NOT add dependencies here — use requirements.txt instead.

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends.legacy:build"

# hydrosovereign PyPI package is in hydrosovereign/ subfolder
# It is published separately and does NOT affect this Streamlit app.
