# HSAE v6.0.14 - Streamlit Cloud requirements
# Core packages required for the app to start. Heavy/optional satellite
# packages (planetary-computer, rioxarray, pystac-client) are imported
# lazily inside try/except at runtime and are intentionally NOT pinned
# here, so a build failure in those never blocks the whole install.

# Core UI
streamlit>=1.35.0

# Data science
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.12.0

# Visualization
plotly>=5.18.0
folium>=0.16.0
streamlit-folium>=0.20.0

# Machine learning
scikit-learn>=1.4.0

# HTTP / APIs (Open-Meteo, GloFAS, USGS, GRACE-FO)
requests>=2.31.0

# Export (Excel reports)
openpyxl>=3.1.0

# Google Earth Engine (Direct GEE mode)
earthengine-api>=0.1.400
