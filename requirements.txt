# ════════════════════════════════════════════════════════
# HSAE v6.01 — Streamlit App Requirements
# HydroSovereign AI Engine
# Author: Seifeldin M.G. Alkedir — University of Khartoum
# ════════════════════════════════════════════════════════

# ── Core UI ──────────────────────────────────────────────
streamlit>=1.35.0

# ── Data Science ─────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# ── Visualization ────────────────────────────────────────
plotly>=5.18.0
folium>=0.16.0
streamlit-folium>=0.20.0
matplotlib>=3.7.0

# ── Machine Learning ─────────────────────────────────────
scikit-learn>=1.3.0

# ── HTTP / APIs (Open-Meteo, GloFAS, USGS) ──────────────
requests>=2.31.0

# ── Export ───────────────────────────────────────────────
openpyxl>=3.1.0

# ── Alerts ───────────────────────────────────────────────
schedule>=1.2.0
python-telegram-bot>=20.0

# NOTE: earthengine-api is NOT listed here intentionally.
# GEE requires OAuth2 authentication which is not available
# on Streamlit Cloud. The app handles this gracefully with
# a fallback message when GEE is unavailable.
# To use GEE locally: pip install earthengine-api
#                     earthengine authenticate
